A window object merely acts as a single window
Windows hold objects

Each window will have a tile system

Window will have:
	Topbar (reserved) -> renders menu items
	Footbar (reversed) -> renders statistics

	Tiles:
		Left:
			upper/lower/corner
		Right:
			upper/lower/corner
		Centre:
			left/right
		Bottom:
			left/right

