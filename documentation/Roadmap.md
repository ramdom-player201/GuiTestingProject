# Step 1:		[ ]
		setup vulkan and be able to render a triangle
		ensure windowing system is functional, and windows can be resized
# Step 2:		[ ]
		create UI functions to render basic shapes and text
		build a basic test gui
		start on input system
# Step 3:		[ ]
		start building program UI
		get log manager working, and provide an interface
		get project selection menu working
		get primary editor window working
# Step 4:		[ ]
		basic 3D features
		render cube
		basic camera controls