# Localisation

## Description


## Implementation

Program will need to store a map of txtKey::localisedText where txtKey is a gui element's text and localisedText is loaded from data.

LocalisationFolder:
- English_UK.lang		<- default language used by project
- TestLanguage.lang		<- debug file used for testing alternate language rendering